<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        La unica salida a la red: preguntarle al sitio del proyecto, una vez al
        dia, si hay una version mas nueva. Es un GET a un archivo estatico que
        no manda nada, y se apaga desde Ajustes. La app no se instala desde
        Play, asi que sin esto nadie se entera de una actualizacion.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".OllinApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/Theme.Ollin">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Ollin">
            <!--
                Solo el lanzador. No hay filtro ACTION_VIEW para "Abrir con Ollin
                Finanzas" desde un gestor de archivos porque nadie leeria el Intent
                entrante: elegir el archivo abriria el tablero y lo ignoraria. El
                dia que se atienda, la URI llega de otra app y hay que tratarla como
                no confiable: importar solo tras confirmacion explicita, nunca al
                vuelo.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".data.notify.RecordatorioReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.notify.ArranqueReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
